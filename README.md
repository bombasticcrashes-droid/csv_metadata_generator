# csv_metadata_generator
This is a project which takes your given api and collects photos from the users and sends requests to the model with some strict instructions and the model then generates the required metadata for the given picture which can then be exported in csv files 
